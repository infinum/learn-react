name: Run tests

on: [push, pull_request]

steps:
  - uses: actions/checkout@v2
  - name: Use Node.js
    uses: actions/setup-node@v2
    with:
      node-version: "12.x"
  - name: Install dependencies
    run: npm ci
  - name: Run tests
    run: npm run test
