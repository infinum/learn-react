import LearnReact from "../../solutions/LearnReact";

let container;

beforeEach(() => {
  container = document.createElement("main");
});

test("The function renders the element.", () => {
  // YOUR TEST GOES HERE
});

test("The function renders the element as a child of the container", () => {
  // YOUR TEST GOES HERE
});

test("The function handles multiple children that can be nested", () => {
  // YOUR TEST GOES HERE
});

test("The function renders nothing if the element is null.", () => {
  // YOUR TEST GOES HERE
});
