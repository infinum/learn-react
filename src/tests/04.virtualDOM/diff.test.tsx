import { waitFor } from "@testing-library/dom";

import LearnReact from "../../solutions/LearnReact";

let container: HTMLElement;

beforeEach(() => {
  container = document.createElement("main");
});

test("Component is rendered with children and type and rerenders correctly after state change", async () => {
  // YOUR TEST GOES HERE
});

test("On rerender, the class of the component changes", async () => {
  // YOUR TEST GOES HERE
});

test("rOn rerender, the style of the component changes", async () => {
  // YOUR TEST GOES HERE
});

test("On rerender, values in other children in the components change", async () => {
  // YOUR TEST GOES HERE
});
